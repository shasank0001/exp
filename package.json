{
  "name": "ml-copilot",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first ML engineering workspace with swappable agent engines",
  "main": "desktop/main.cjs",
  "scripts": {
    "start": "electron .",
    "check": "node --check desktop/main.cjs && node --check desktop/preload.cjs && node --check desktop/host.js",
    "test": "node --test tests/*.test.cjs",
    "test:desktop": "node scripts/smoke-desktop.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "electron": "44.4.1",
    "playwright": "1.63.0"
  }
}
