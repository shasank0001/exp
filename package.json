{
  "name": "ml-copilot",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first ML engineering workspace with swappable agent engines",
  "main": "desktop/main.cjs",
  "scripts": {
    "start": "electron .",
    "check": "node --check desktop/main.cjs && node --check desktop/preload.cjs && node --check desktop/host.js && node --check desktop/policy.cjs && node --check engines/pi-adapter.cjs",
    "test": "node --test tests/policy.test.cjs tests/pi-adapter.test.cjs",
    "test:desktop": "node --test tests/desktop-sandbox.test.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "0.73.1",
    "electron": "44.4.1",
    "playwright": "1.63.0"
  }
}
